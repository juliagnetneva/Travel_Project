import styled from "styled-components";
import {FlexRow} from "../../../shared";

export const FavItem = styled(FlexRow)`
  width: 100%;
  border-bottom: 1px solid #777777;
  margin-bottom: 0.7rem;
  text-align: start;
`